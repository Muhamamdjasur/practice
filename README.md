# PRACTICE PROJECT

Learning Git  and Python Foundation

##GIT COMMANDS:
```

git config --global.user
git config --global.name "your_name"
git config --global.email "your_email@example.com"
git init

git status
git add .
git commit -m "feat: Initial Set Up"

git remote add origin https://github_address
git push origin master
git pull origin master
```